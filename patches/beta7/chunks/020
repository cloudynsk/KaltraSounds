ume(float multiplier) {
+        float safe = Float.isFinite(multiplier) ? Math.max(0.0f, multiplier) : 1.0f;
+        return new SoundSpec(key, volume * safe, pitch, delay, radius, category, triggerPermission, listenPermission, ignoresDisabled);
+    }
+
     static List<SoundSpec> fromRule(ConfigurationSection rule) {
         if (rule == null || !rule.getBoolean("Enabled", false)) return Collections.emptyList();
         ConfigurationSection sounds = rule.getConfigurationSection("Sounds");
diff --git a/src/main/resources/Sounds/biomes.yml b/src/main/resources/Sounds/biomes.yml
index 0a2bcb0..abf2cbb 100644
--- a/src/main/resources/Sounds/biomes.yml
+++ b/src/main/resources/Sounds/biomes.yml
@@ -1 +1,3 @@
+# Biome loop example and settings are documented in LOOPING.md.
+# Fades are replay-volume ramps; Minecraft cannot alter an already-playing sound's volume.
 Version: 'KaltraSounds-1'
diff --git a/src/main/resources/Sounds/regions.yml b/src/main/resources/Sounds/regions.yml
index 0a