Location origin,
+                                     Collection<? extends Player> allowedRecipients, float volumeMultiplier) {
+        if (!Float.isFinite(volumeMultiplier) || volumeMultiplier < 0.0f) return false;
         List<SoundSpec> specs = SoundSpec.fromRule(rule);
         if (specs.isEmpty()) return false;
         boolean dispatched = false;
-        for (SoundSpec spec : specs) dispatched |= play(spec, actor, origin, allowedRecipients);
+        for (SoundSpec spec : specs) dispatched |= play(spec.scaledVolume(volumeMultiplier), actor, origin, allowedRecipients);
         return dispatched;
     }
 
diff --git a/src/main/java/net/kaltra/sounds/SoundSpec.java b/src/main/java/net/kaltra/sounds/SoundSpec.java
index a6d8513..6ca1194 100644
--- a/src/main/java/net/kaltra/sounds/SoundSpec.java
+++ b/src/main/java/net/kaltra/sounds/SoundSpec.java
@@ -17,6 +17,11 @@ record SoundSpec(
         String listenPermission,
         boolean ignoresDisabled
 ) {
+    SoundSpec scaledVol